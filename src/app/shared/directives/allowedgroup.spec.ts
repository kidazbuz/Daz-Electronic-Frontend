import { Allowedgroup } from './allowedgroup';

describe('Allowedgroup', () => {
  it('should create an instance', () => {
    const directive = new Allowedgroup();
    expect(directive).toBeTruthy();
  });
});
